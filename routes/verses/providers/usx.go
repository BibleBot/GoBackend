package providers

// This will process requests on versions that we have USX files for.
// Local USX files are preferable to scraping so we aren't so dependent on other services.

func usx() {
	return
}
