package interfaces

// This file will import the interfaces for use.
// We should probably implement a struct defining a proper interface to ensure that each has a Search(), Fetch(), etc.

func getResult() {
	return
}
