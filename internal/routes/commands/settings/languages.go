package settings

// Handler for all language-related commands.
// This should be quite similar to versions.go as they basically do the same thing, give or take a few features.

func languages() {
	return
}
